{
	"name": "@moll/fetch-parse",
	"version": "0.3.0",
	"description": "Fetch API mixin to buffer and parse response bodies. Supports media type patterns for content type specific custom parsers",
	"keywords": [
		"fetch"
	],
	"homepage": "https://github.com/moll/js-fetch-parse",
	"bugs": "https://github.com/moll/js-fetch-parse/issues",

	"author": {
		"name": "Andri Möll",
		"email": "andri@dot.ee",
		"url": "http://themoll.com"
	},

	"repository": {
		"type": "git",
		"url": "git://github.com/moll/js-fetch-parse.git"
	},

	"licenses": [{
		"type": "LAGPL",
		"url": "https://github.com/moll/js-fetch-parse/blob/master/LICENSE"
	}],

	"main": "index.js",
	"scripts": {"test": "make test"},

	"dependencies": {
		"medium-type": "^1.0.0"
	},

	"devDependencies": {
		"mocha": ">= 2.5.3 < 3",
		"co-mocha": ">= 1.0.0 < 2",
		"must": ">= 0.13.2 < 0.14",
		"sinon": ">= 1.15.4 < 2",
		"whatwg-fetch": ">= 0.10.1 < 0.11",
		"require-guard": ">= 1.0.0 < 2"
	}
}
